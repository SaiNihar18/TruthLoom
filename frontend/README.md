# TruthLoom frontend

React and Vite. Talks to the backend at `http://localhost:8000` (see `src/api.js`).

```
npm install
npm run dev
```

See the repository root README for the full setup and run instructions.
